{
  "name": "cll-fullstack-app",
  "version": "1.0.0",
  "description": "Full-stack app with React and Node.js for Lazada integration",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build": "cd client && npm run build",
    "build:client": "cd client && npm run build",
    "start:server": "cd server && npm start",
    "deploy": "cd client && npm run build && gh-pages -d dist",
    "clean": "rm -rf node_modules client/node_modules server/node_modules client/dist",
    "reinstall": "npm run clean && npm run install:all"
  },
  "keywords": [
    "react",
    "nodejs",
    "express",
    "lazada",
    "fullstack"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "gh-pages": "^6.3.0"
  }
}